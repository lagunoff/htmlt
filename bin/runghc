#!/usr/bin/env sh
DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" >/dev/null 2>&1 && pwd )"

args=$(for a in $(cabal-cargs -c $DIR/../massaraksh.cabal); do echo "--ghc-arg=$a"; done | tr '\r\n' ' ')

cached-nix-shell $DIR/../default.nix -A shells.ghc --run "runghc $args $@"
