#!/usr/bin/env sh
DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" >/dev/null 2>&1 && pwd )"

CABAL_ARGS=$(cabal-cargs -c $DIR/../massaraksh.cabal)
FORWARD_ARGS=$@

cached-nix-shell $DIR/../default.nix -A shells.ghc --run "ghc $CABAL_ARGS -O0 --interactive $FORWARD_ARGS"
